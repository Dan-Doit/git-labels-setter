{
  "name": "label",
  "version": "1.0.0",
  "description": "git custom label maker",
  "main": "index.js",
  "scripts": {
    "get": "node main.js",
    "set": "dotenv cross-var github-label-sync --access-token %TOKEN% --labels labels.json %ACCOUNT%/%REPOSITORY%"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cross-var": "^1.1.0",
    "dotenv": "^16.0.2",
    "dotenv-cli": "^6.0.0",
    "github-label-sync": "^2.2.0"
  }
}
